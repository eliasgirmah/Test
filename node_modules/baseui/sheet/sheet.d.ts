/// <reference types="react" />
import type { SheetProps } from './types';
export declare const Sheet: ({ children, content, description, draggable, leadingAction, overrides, progressBar, title, topPosition, trailingAction, }: SheetProps) => JSX.Element;
export default Sheet;
